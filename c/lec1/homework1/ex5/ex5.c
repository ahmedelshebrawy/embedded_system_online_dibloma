/*
 * ex5.c
Write C Program to Find ASCII Value of a Character

 *      Author: aelsh
 */

#include<stdio.h>
int main(){

	char x;
	printf("enter a character :"); //a character
	fflush(stdout);
		fflush(stdin);
	scanf("%c",&x);
	fflush(stdout);
		fflush(stdin);
	printf("ASCII value of %C =%d",x,x); //ASCII value
	fflush(stdout);
		fflush(stdin);
    return 0;
}
